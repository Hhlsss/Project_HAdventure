# 《小H的冒险》局域网联机使用说明

## 🎮 功能概述

本联机系统实现了本地局域网内的多人游戏功能，第一个连接的玩家自动成为主机，其他玩家可以加入主机的游戏世界进行实时联机游戏。

## 📋 系统要求

- 操作系统：Windows 10/11
- Node.js 版本：14.0.0 或更高版本
- 网络环境：同一局域网内的多台设备
- Cocos Creator 3.x 项目

## 🚀 快速开始

### 1. 安装 Node.js 依赖

在项目根目录打开命令行，执行：

```bash
npm install
```

如果没有安装 npm，请先安装 Node.js：https://nodejs.org/

### 2. 启动服务器

```bash
npm start
```

或者直接运行：

```bash
node local-server.js
```

### 3. 启动成功标志

看到以下输出表示服务器启动成功：

```
🚀 《小H的冒险》联机服务器启动成功！
📡 HTTP服务器运行在: http://192.168.1.115:8080
🔌 WebSocket服务器运行在: ws://192.168.1.115:7457
⏰ 启动时间: [当前时间]
```

## 🌐 客户端配置

### 1. Cocos Creator 项目配置

1. **添加 NetworkManager 组件**
   - 在游戏场景中创建一个空节点，命名为 "NetworkManager"
   - 将 `NetworkManager.ts` 脚本挂载到该节点
   - 在 Inspector 中将其他玩家预制体拖拽到 `Other Player Prefab` 字段

2. **配置 Hero 组件**
   - 确保玩家角色节点上已有 `hero.ts` 脚本
   - 将 NetworkManager 节点拖拽到 Hero 组件的 `Network Manager` 字段

3. **创建其他玩家预制体**
   - 复制玩家角色节点作为预制体
   - 移除输入控制相关组件
   - 确保包含 Hero 组件用于动画同步

### 2. 防火墙设置

确保防火墙允许以下端口：
- **7457** (WebSocket 通信端口)
- **8080** (HTTP 状态页面端口)

## 🎯 使用流程

### 主机玩家（第一个连接）

1. 启动服务器
2. 在 Cocos Creator 中运行游戏项目
3. 自动获得主机身份，控制台会显示：`👑 你成为了主机玩家`
4. 正常开始游戏，其他玩家可以加入你的世界

### 客户端玩家（后续连接）

1. 确保服务器已启动
2. 在同一局域网内的其他设备上运行相同的 Cocos Creator 项目
3. 自动加入主机玩家的游戏世界
4. 可以看到主机玩家的实时位置和动作
5. 自己的位置和动作也会同步给其他玩家

## 🔧 技术特性

### 实时同步
- **位置同步**：每 10ms 同步一次玩家位置（高频率，更流畅）
- **动画同步**：实时同步玩家的动作状态（idle、walk、run、jump）
- **攻击同步**：攻击事件和方向实时广播

### 自动主机管理
- 第一个连接的玩家自动成为主机
- 主机断开连接时，自动选择下一个玩家作为新主机
- 所有玩家都能看到当前的主机信息

### 连接管理
- 自动重连机制：断线后 5 秒自动重连
- 连接状态监控：实时显示连接状态
- 优雅的玩家加入/离开处理

## 🛠️ 故障排除

### 常见问题

**Q: 无法连接到服务器**
A: 
1. 检查服务器是否正常启动
2. 确认 IP 地址 `192.168.1.115` 是否正确
3. 检查防火墙设置，确保允许 7457 端口
4. 确认设备在同一局域网内

**Q: 看不到其他玩家**
A:
1. 检查 NetworkManager 组件是否正确配置
2. 确认 Other Player Prefab 是否已设置
3. 查看浏览器控制台是否有错误信息

**Q: 主机身份不正确**
A:
1. 第一个连接的玩家自动成为主机
2. 刷新页面可以重新分配主机身份
3. 主机断开后会自动切换

### 调试方法

1. **服务器日志**：查看服务器命令行窗口的连接信息
2. **客户端控制台**：在浏览器中按 F12 查看 Console 面板
3. **网络检查**：在浏览器开发者工具的 Network 面板查看 WebSocket 连接状态

## 📱 支持的设备

- **Windows 10/11** 电脑
- **macOS** 电脑
- **Android** 设备（浏览器模式）
- **iOS** 设备（浏览器模式）

## 🔒 安全注意事项

1. **局域网使用**：本服务器设计用于本地局域网，不建议暴露到公网
2. **端口管理**：建议只在游戏时开放相关端口
3. **网络监控**：注意观察连接的设备数量

## 📞 技术支持

如遇到问题，请检查：
1. Node.js 版本是否满足要求
2. 所有依赖是否正确安装
3. 网络连接是否正常
4. 防火墙设置是否正确

---

**祝您游戏愉快！** 🎮✨