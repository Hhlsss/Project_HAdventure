# 房间系统使用说明

## 🎯 功能概述

基于房间号的联机系统，玩家可以选择创建房间或加入已有房间，实现更精确的多人游戏匹配。

## 🏗️ 系统架构

### 服务器端
- **房间管理**: 自动生成房间号，支持创建/加入/离开
- **房主机制**: 第一个创建房间的玩家为房主，离开后自动转交
- **自动清理**: 房间为空时自动删除，释放资源

### 客户端
- **UI界面**: Host按钮、Join按钮、房间号输入框、状态显示
- **本地游戏**: 默认单机模式，可选择联机
- **状态管理**: 实时显示连接状态和房间信息

## 🚀 使用流程

### 创建房间（成为房主）
1. **启动游戏**: 运行Cocos Creator项目
2. **选择Host**: 点击"Host"按钮
3. **获得房间号**: 系统自动生成房间号（如1001）
4. **分享房间号**: 将房间号告诉朋友
5. **开始游戏**: 等待其他玩家加入

### 加入房间（成为访客）
1. **启动游戏**: 运行Cocos Creator项目
2. **输入房间号**: 在输入框中输入朋友的房间号
3. **点击Join**: 点击"Join"按钮
4. **等待加入**: 系统验证房间号并加入
5. **开始游戏**: 与房间内其他玩家联机游戏

### 错误处理
- **房间不存在**: 输入错误的房间号会显示"房间不存在"
- **连接失败**: 网络问题会提示重试
- **房间满员**: 虽然当前不限制人数，但可扩展

## 🎮 UI界面说明

### 组件说明
```
┌─────────────────────────┐
│      联机菜单          │
├─────────────────────────┤
│  [ Host ]  [ Join ]   │  ← 按钮组件
│  房间号: [输入框]     │  ← 输入框组件  
│  状态: [等待操作...]   │  ← 状态标签
│  房间号: [1001]      │  ← 房间号标签（加入后显示）
└─────────────────────────┘
```

### 状态显示
- **白色**: 正常等待状态
- **黄色**: 正在处理请求
- **绿色**: 操作成功
- **红色**: 错误提示

## 🔧 技术实现

### 服务器消息类型
```javascript
// 创建房间
{ type: 'create_room' }

// 加入房间  
{ type: 'join_room', roomId: 1001 }

// 房间创建成功
{ type: 'room_created', roomId: 1001, isHost: true }

// 加入房间成功
{ type: 'room_joined', roomId: 1001, isHost: false }

// 房间不存在
{ type: 'room_not_found', roomId: 1001 }
```

### 客户端组件
- **RoomUI**: 管理UI界面和用户交互
- **NetworkManager**: 处理网络通信和房间逻辑
- **hero**: 玩家控制和网络同步

## 📋 配置要求

### 场景配置
1. **NetworkManager节点**: 挂载NetworkManager.ts脚本
2. **RoomUI节点**: 挂载RoomUI.ts脚本  
3. **UI组件**: 
   - Button (Host, Join)
   - EditBox (房间号输入)
   - Label (状态显示, 房间号显示)

### 脚本绑定
```typescript
// RoomUI.ts 组件绑定
@property({ type: Button }) public hostButton: Button | null = null;
@property({ type: Button }) public joinButton: Button | null = null;  
@property({ type: EditBox }) public roomInput: EditBox | null = null;
@property({ type: Label }) public statusLabel: Label | null = null;
@property({ type: Label }) public roomLabel: Label | null = null;
```

## 🎯 游戏流程

### 单机模式（默认）
- 玩家启动游戏后自动进入单机模式
- 可以随时选择创建或加入房间
- 不需要网络连接

### 联机模式
1. **选择操作**: 创建房间或加入房间
2. **建立连接**: 连接到本地服务器
3. **房间同步**: 房间内玩家互相可见
4. **游戏开始**: 正常的多人游戏体验

## 🔍 测试验证

### 基本功能测试
- [ ] 创建房间成功，显示房间号
- [ ] 输入正确房间号，成功加入
- [ ] 输入错误房间号，提示不存在
- [ ] 多人加入同一房间，互相可见

### 错误处理测试
- [ ] 网络断开时的重连机制
- [ ] 重复操作的处理
- [ ] 边界情况的处理（空输入等）

### 性能测试
- [ ] 多房间并发创建
- [ ] 大量玩家加入/离开
- [ ] 长时间运行稳定性

## 🚀 扩展功能

### 近期扩展
1. **房间列表**: 显示所有可用房间
2. **密码保护**: 房间可设置密码
3. **玩家上限**: 限制房间最大人数
4. **游戏模式**: 房间可选择不同游戏模式

### 长期扩展  
1. **匹配系统**: 自动匹配空闲房间
2. **好友系统**: 好友间快速邀请
3. **排行榜**: 房间内玩家排行
4. **观战模式**: 允许玩家观战

## 🛠️ 故障排除

### 常见问题

**Q: 点击Host按钮没有反应**
A: 检查NetworkManager组件是否正确连接服务器

**Q: 输入房间号后提示不存在**
A: 确认房间号正确，服务器正常运行

**Q: 加入房间后看不到其他玩家**
A: 检查预制体配置和网络同步

**Q: 状态显示一直是"正在处理"**
A: 检查网络连接和服务器响应

### 调试命令
```javascript
// 在浏览器控制台检查房间状态
const networkManager = cc.find("NetworkManager")?.getComponent("NetworkManager");
console.log("连接状态:", networkManager?.getConnectionStatus());

// 检查UI状态  
const roomUI = cc.find("RoomUI")?.getComponent("RoomUI");
console.log("是否在房间:", roomUI?.isInRoomState());
```

## 📞 技术支持

如遇到问题，请提供：
1. 服务器控制台日志
2. 客户端浏览器控制台日志
3. 具体操作的步骤
4. 网络环境信息

---

**房间系统让联机更简单！房间号，一键开黑！** 🏠✨