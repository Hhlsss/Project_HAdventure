# 解决IP地址变化问题

## 问题描述
当您的网络环境发生变化时（例如更换网络、路由器重启等），您的计算机IP地址可能会改变。由于《小H的冒险》游戏之前使用硬编码的IP地址，这会导致昨天还能正常连接的服务器今天无法连接。

## 解决方案

### 方法一：使用游戏内置IP设置功能（推荐）
1. 打开游戏后，在房间界面按 `I` 键
2. 在弹出的对话框中输入服务器IP地址
3. 点击确定保存IP地址

### 方法二：使用IP检测工具
1. 在项目根目录中打开 `ip-detection.html` 文件
2. 点击"测试IP地址"按钮，找到可用的服务器IP
3. 在游戏中按 `I` 键，输入找到的IP地址

### 方法三：手动查找IP地址
1. 在运行服务器的计算机上，打开命令提示符（cmd）
2. 输入 `ipconfig` 并回车
3. 找到"IPv4 地址"，例如：192.168.1.115
4. 在游戏中按 `I` 键，输入这个IP地址

## 常见网络环境下的IP地址

### 家庭WiFi网络
- 通常是 192.168.1.x 或 192.168.0.x
- 例如：192.168.1.115, 192.168.0.115

### 公司/学校网络
- 可能是 10.x.x.x 或 172.16.x.x - 172.31.x.x
- 例如：10.0.0.115, 172.16.0.115

### 本地测试
- 如果游戏和服务器在同一台计算机上，可以使用：
  - localhost
  - 127.0.0.1

## 其他解决方案

### 设置静态IP地址（高级用户）
如果您经常遇到IP变化问题，可以考虑设置静态IP地址：

1. 打开控制面板 > 网络和 Internet > 网络和共享中心
2. 点击当前连接的网络
3. 点击"属性"
4. 选择"Internet 协议版本 4 (TCP/IPv4)"
5. 选择"使用下面的 IP 地址"
6. 输入IP地址、子网掩码和默认网关
7. 保存设置

### 使用端口转发
如果您想从外部网络连接到游戏服务器，可以设置端口转发：

1. 登录到路由器管理界面
2. 找到"端口转发"或"虚拟服务器"设置
3. 添加规则：外部端口7457转发到服务器IP的7457端口
4. 保存设置

## 故障排除

### 连接失败
1. 确认服务器已启动（在cmd目录中运行"启动服务器.bat"）
2. 确认IP地址正确
3. 检查防火墙设置
4. 尝试关闭杀毒软件

### 找不到IP地址
1. 确认服务器和客户端在同一网络
2. 使用 `ip-detection.html` 工具自动检测
3. 在命令提示符中运行 `ipconfig` 查看IP

## 联系支持
如果您仍然遇到问题，请提供以下信息：
- 您的网络环境（家庭、学校、公司等）
- 服务器和客户端是否在同一网络
- 错误信息的完整截图